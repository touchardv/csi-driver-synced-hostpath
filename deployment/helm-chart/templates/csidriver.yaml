apiVersion: storage.k8s.io/v1
kind: CSIDriver
metadata:
  name: {{ .Values.driver.name }}
spec:
  attachRequired: true
  fsGroupPolicy: File
  podInfoOnMount: false
  volumeLifecycleModes:
    - Persistent

apiVersion: v1
kind: Service
metadata:
  name: csi-synced-hostpath-fileserver
  namespace: kube-system
spec:
  ports:
    - name: grpc
      port: 50051
      protocol: TCP
  selector:
    app: csi-synced-hostpath-controller
  type: ClusterIP
